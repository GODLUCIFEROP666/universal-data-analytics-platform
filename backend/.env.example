HOST=127.0.0.1
PORT=8001
CORS_ORIGINS=http://localhost:5173
JWT_SECRET=change-this-to-a-random-secret-in-production
DATABASE_PATH=./data.db

# Initial admin password used ONLY during first database initialization.
# ALWAYS change this value before deploying to production!
ADMIN_INITIAL_PASSWORD=ChangeMeOnFirstLogin#123

